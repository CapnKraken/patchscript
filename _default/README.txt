When creating a new project via the command line, Patchscript looks for a project folder called '_default' in the root directory.
It will copy over whatever is in this folder into the new project.
If the _default folder does not exist, it will create an empty project.